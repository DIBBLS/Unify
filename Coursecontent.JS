// Coursecontent.JS — legacy file kept as stub to avoid reference errors.
// All course content is now managed via the Admin panel and stored in Firestore.

window.UNIFY_COURSE_CONTENT = {};
window.generateCourseStubs = function() {};
window.getWeekResources = function() { return {}; };
window.getCourseContent = function() { return null; };
window.topicHasContent = function() { return false; };
window.getTopicContentFile = function() { return null; };
